<!DOCTYPE html>
<html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Kendaraan</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="parkir-body"><div class="parkir-shell"><aside class="sidebar"><div class="brand"><div class="brand-dot">P</div><div class="brand-title">Precision Flow</div></div>
<nav class="menu"><a class="menu-item" href="{{ route('dashboard') }}">Dashboard</a><a class="menu-item" href="{{ route('users.index') }}">User Management</a><a class="menu-item" href="{{ route('tarif-parkir.index') }}">Tarif Parkir</a><a class="menu-item" href="{{ route('area-parkir.index') }}">Area Parkir</a><a class="menu-item active" href="{{ route('kendaraan.index') }}">Kendaraan</a><a class="menu-item" href="{{ route('log-aktivitas.index') }}">Log Aktivitas</a></nav></aside>
<main class="main-content"><section class="heading-block"><h1>Kendaraan</h1><p>Kelola data kendaraan terdaftar.</p></section>@if(session('success'))<div class="alert-success">{{ session('success') }}</div>@endif
<section class="table-card"><div class="table-head"><h2>Daftar Kendaraan</h2><form class="table-tools" method="GET" action="{{ route('kendaraan.index') }}"><input type="text" name="q" value="{{ $q }}" placeholder="Cari plat/jenis/pemilik..."><button type="submit">Cari</button><a class="btn-primary" href="{{ route('kendaraan.create') }}">+ Tambah Kendaraan</a></form></div>
<div class="table-wrap"><table><thead><tr><th>ID</th><th>PLAT</th><th>JENIS</th><th>WARNA</th><th>PEMILIK</th><th>USER</th><th>AKSI</th></tr></thead><tbody>@forelse($kendaraan as $item)<tr><td>{{ $item->id_kendaraan }}</td><td>{{ $item->plat_nomor }}</td><td>{{ $item->jenis_kendaraan }}</td><td>{{ $item->warna }}</td><td>{{ $item->pemilik }}</td><td>{{ $item->user?->nama ?? '-' }}</td><td><div class="row-actions"><a class="btn-ghost" href="{{ route('kendaraan.edit', $item->id_kendaraan) }}">Edit</a><form method="POST" action="{{ route('kendaraan.destroy', $item->id_kendaraan) }}" onsubmit="return confirm('Hapus kendaraan ini?')">@csrf @method('DELETE')<button class="btn-danger" type="submit">Hapus</button></form></div></td></tr>@empty<tr><td colspan="7" class="empty-row">Belum ada data kendaraan.</td></tr>@endforelse</tbody></table></div></section></main></div></body></html>
